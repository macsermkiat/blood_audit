---
title: Policy summary
description: Paraphrased summary of the 3-tier Hb policy applied by the audit. Authoritative text lives in the internal KCMH documents.
---

import AlgorithmDecisionFlow from "../../../../components/AlgorithmDecisionFlow.astro";
import AtAGlance from "../../../../components/AtAGlance.astro";
import HbTierMatrix from "../../../../components/HbTierMatrix.astro";

:::caution
The text below is a **paraphrased operational summary**. The authoritative
clinical policy is KCMH PR 17.2 and the internal 3-tier Hb specification
(`policy/policy.md` in the internal Bloodbank policy bundle). Always
defer to those documents in case of conflict.
:::

<AtAGlance
  items={[
    { label: "Plain rule", value: "Hb < 7.0 g/dL is APPROPRIATE before cohort-specific review routes." },
    { label: "Checked data", value: "Lab Hb, ICD-10, ICD-9-CM procedures, MED, blood-order pattern." },
    { label: "Vitals", value: "Supporting evidence only; never a standalone bypass.", tone: "warn" },
    { label: "Review route", value: "Unknown or non-threshold contexts become NEEDS_REVIEW only when Hb is ≥ 7.0.", tone: "ok" },
  ]}
/>

## Source documents (internal)

- KCMH PR 17.2 — institutional RBC transfusion guideline.
- AABB 2023 — international restrictive-transfusion guidance.
- KCMH internal 3-tier Hb specification (`policy/policy.md`).

## The 3-tier rule

<HbTierMatrix />

## Decision flow

<AlgorithmDecisionFlow />

## Bypass and review paths

The plain Hb tier is not the first check. After excluding missing Hb, the
algorithm first applies the global low-Hb rule and then looks for other
high-precedence evidence:

- **Hb < 7.0 g/dL** → deterministic `APPROPRIATE` before cohort routing.
- **MTP clinical context** → deterministic `APPROPRIATE`.
- **Peri-procedural ≤ 6h before order** → deterministic `APPROPRIATE`.
- **Pre-op crossmatch ≤ 72h after order** → deterministic `APPROPRIATE`.
- **Delta-Hb trigger** → deterministic `APPROPRIATE` when the Hb drop meets:
  - ≥ 1.5 g/dL drop within 6 hours
  - ≥ 2.0 g/dL drop within 12 hours
  - ≥ 2.5 g/dL drop within 24 hours
- **Hemodilution or isolated low Hb without trend** → `NEEDS_REVIEW` only
  after the Hb < 7.0 rule has already had first chance to classify.
- **Unknown or non-threshold clinical context** → `NEEDS_REVIEW` when
  Hb is ≥ 7.0.

Vital-sign abnormalities alone (low BP, tachycardia) do not trigger the
bypass.

## Exclusions and insufficient evidence

- Pediatric patients (age < 15) — different policy applies.
- Non-RBC products (LPRC, LDPRC, SDR).
- ICD-10 conditions that take the patient out of scope per PR 17.2.

Orders without a documented pre-transfusion Hb are **not** classified as
appropriate or inappropriate. They are retained as `INSUFFICIENT_EVIDENCE`
so the QI committee can see the documentation gap.

## Why paraphrased here

PR 17.2 is an internal hospital document. To respect institutional
authorship, this public site paraphrases the operational rule rather than
republishing the source. The audit code reflects the authoritative spec.
