---
title: blood_audit
description: ระบบตรวจสอบการให้เลือดแดงเข้มข้น (RBC) สำหรับโรงพยาบาลจุฬาฯ เทียบกับ PR 17.2 และ AABB 2023
template: splash
hero:
  tagline: ระบบออดิทย้อนหลังการให้เลือดแดงเข้มข้นในผู้ป่วยในผู้ใหญ่ ณ KCMH เทียบกับ PR 17.2 + AABB 2023
  actions:
    - text: ติดตั้งและรันออดิทครั้งแรก
      link: /blood_audit/th/operators/install/
      icon: right-arrow
      variant: primary
    - text: ดูบน GitHub
      link: https://github.com/macsermkiat/blood_audit
      icon: external
      variant: minimal
---

import AudienceTiles from "../../../components/AudienceTiles.astro";
import AtAGlance from "../../../components/AtAGlance.astro";
import PipelineDiagram from "../../../components/PipelineDiagram.astro";
import QiNotSamdBanner from "../../../components/QiNotSamdBanner.astro";

<QiNotSamdBanner />

<AtAGlance
  title="สรุปสั้น"
  items={[
    { label: "ขอบเขต", value: "ออดิทย้อนหลังเพื่อ QI เท่านั้น ไม่ใช่ alert ณ จุดดูแล", tone: "warn" },
    { label: "ข้อมูลเข้า", value: "ชุด CSV จาก HOSxP: order, Lab, ICD, procedure, MED, notes" },
    { label: "กฎหลัก", value: "3-tier Hb classifier แบบ deterministic พร้อมกลุ่มบริบททางคลินิกและ bypass ที่มีหลักฐาน" },
    { label: "ผลลัพธ์วันนี้", value: "DuckDB + Parquet snapshot; AuditRow/report ใน Phase 1.5" },
  ]}
/>

## blood_audit ทำอะไร

blood_audit อ่านข้อมูลส่งออกจาก HOSxP ที่เสร็จสมบูรณ์แล้ว
เปรียบเทียบการให้เลือดแดงเข้มข้น (RBC, red blood cell) แต่ละครั้ง
กับนโยบายฮีโมโกลบิน (Hb) แบบ 3-ชั้น แล้วเขียนผลออดิท 1 แถว
ต่อหน่วยเลือด 1 หน่วย ลงใน audit store

ค่า `final_classification` ที่บันทึกสำหรับการให้เลือดแต่ละครั้ง จะเป็น
1 ใน 5 ค่า:

- `APPROPRIATE` — คำสั่งตรงกับนโยบาย
- `INAPPROPRIATE` — คำสั่งไม่ตรงกับนโยบาย
- `POTENTIALLY_INAPPROPRIATE` — Hb สูงหรือมีสัญญาณที่ควรตรวจต่อ
- `NEEDS_REVIEW` — LLM ทำงานแล้ว แต่คำตอบต้องให้คนทบทวน
  (เหตุผลไม่ชัด, quote-grounding ล้มเหลว, หรือ confidence ต่ำ)
- `INSUFFICIENT_EVIDENCE` — เอกสารหลักฐานไม่พอ ไม่ใช่ `INAPPROPRIATE`
  โดยตั้งใจ

แถวที่ deterministic classifier ส่งต่อให้ LLM จะผ่าน quote-grounding
แบบ fail-closed 6 ชั้น ถ้า grounding ล้มเหลว ผลจะเป็น `NEEDS_REVIEW`
ไม่ใช่ผลลัพธ์ที่เผยแพร่

## สถานะ Phase 1

ตอนนี้ `bba audit` ทำเฉพาะ **ส่วน ingest** ของ pipeline คือแปลงไฟล์ CSV
จาก HOSxP เป็น DuckDB + Parquet ภายใต้
`$BBA_DATA_DIR/audit/<run_id>/` พร้อม run-level idempotency
ส่วน LLM analysis (deterministic classifier → evidence bundle → de-id →
prompt → batch → quote grounding → audit-store write) เขียนเป็นโมดูล
แยกแล้ว แต่จะ **เชื่อมต่อใน Phase 1.5** ดูรายละเอียดที่
[audit command](/blood_audit/en/operators/audit-command/) (เอกสารภาษาอังกฤษ)

## 12 ขั้นตอนของ pipeline

<PipelineDiagram />

## ขั้นถัดไป

<AudienceTiles
  tiles={[
    {
      label: "ผู้ดูแลระบบ",
      audience: "KCMH IT",
      description:
        "ติดตั้งแพ็กเกจ ตั้งค่า env vars 3 ตัว รัน bba ingest แล้วตามด้วย bba audit ส่งชุดข้อมูลให้คณะกรรมการ QI",
      primaryHref: "/blood_audit/th/operators/install/",
      primaryText: "ติดตั้ง",
      secondaryHref: "/blood_audit/th/operators/first-audit-run/",
      secondaryText: "รันออดิทครั้งแรก",
    },
    {
      label: "นักพัฒนา",
      audience: "ผู้ดูแล src/bba/",
      description:
        "12 ขั้นตอนของ pipeline, deterministic 3-tier Hb classifier, quote-grounding 6 ชั้น แบบ fail-closed (เอกสารภาษาอังกฤษ)",
      primaryHref: "/blood_audit/en/developers/architecture/",
      primaryText: "Architecture (EN)",
      secondaryHref: "/blood_audit/en/reference/module-glossary/",
      secondaryText: "Module glossary (EN)",
    },
    {
      label: "ทางคลินิก",
      audience: "คณะกรรมการ QI",
      description:
        "ขอบเขตของการออดิทย้อนหลัง 5 ค่า classification และ 6 ส่วนของรายงาน ไม่ใช่ Software as a Medical Device",
      primaryHref: "/blood_audit/th/clinical/what-this-is/",
      primaryText: "นี่คืออะไร",
    },
  ]}
/>
