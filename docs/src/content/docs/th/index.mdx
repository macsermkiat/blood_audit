---
title: blood_audit
description: ระบบออดิทย้อนหลังการให้เลือดแดงเข้มข้น (RBC) ของโรงพยาบาลจุฬาฯ เทียบกับ PR 17.2 และ AABB 2023
template: splash
hero:
  tagline: ระบบออดิทย้อนหลังการให้เลือดแดงเข้มข้นในผู้ป่วยในผู้ใหญ่ ณ KCMH เทียบกับ PR 17.2 และ AABB 2023
  actions:
    - text: ติดตั้งและรันออดิทครั้งแรก
      link: /blood_audit/th/operators/install/
      icon: right-arrow
      variant: primary
    - text: ดูบน GitHub
      link: https://github.com/macsermkiat/blood_audit
      icon: external
      variant: minimal
---

import AudienceTiles from "../../../components/AudienceTiles.astro";
import AtAGlance from "../../../components/AtAGlance.astro";
import PipelineDiagram from "../../../components/PipelineDiagram.astro";
import QiNotSamdBanner from "../../../components/QiNotSamdBanner.astro";

<QiNotSamdBanner />

<AtAGlance
  title="สรุปสั้น"
  items={[
    { label: "ขอบเขต", value: "ออดิทย้อนหลังเพื่อ QI เท่านั้น ไม่ใช่ระบบแจ้งเตือน ณ จุดดูแล", tone: "warn" },
    { label: "ข้อมูลเข้า", value: "ชุด CSV จาก HOSxP ครอบคลุมคำสั่งเลือด, Lab, ICD, procedure, MED และ note" },
    { label: "กฎหลัก", value: "ตัวจำแนก 3-tier Hb แบบ deterministic พร้อมบริบททางคลินิกและ bypass ที่ต้องมีหลักฐาน" },
    { label: "ผลลัพธ์ตอนนี้", value: "Phase 1 สร้าง DuckDB + Parquet snapshot; AuditRow และรายงานจะมาใน Phase 1.5" },
  ]}
/>

## blood_audit ทำอะไร

blood_audit อ่านชุดข้อมูลที่ส่งออกจาก HOSxP หลังเหตุการณ์ให้เลือด
เสร็จสิ้นแล้ว จากนั้นเปรียบเทียบการให้เลือดแดงเข้มข้น
(RBC, red blood cell) แต่ละครั้งกับนโยบายฮีโมโกลบิน (Hb) แบบ 3 ชั้น
และเตรียมผลออดิท 1 แถวต่อหน่วยเลือด 1 หน่วยสำหรับ audit store

ค่า `final_classification` ที่บันทึกสำหรับแต่ละรายการให้เลือดจะเป็น
1 ใน 5 ค่า:

- `APPROPRIATE` — คำสั่งสอดคล้องกับนโยบาย
- `INAPPROPRIATE` — คำสั่งไม่สอดคล้องกับนโยบาย
- `POTENTIALLY_INAPPROPRIATE` — Hb สูงหรือมีสัญญาณที่ควรตรวจทบทวนต่อ
- `NEEDS_REVIEW` — มีเหตุผลให้คนทบทวนก่อนสรุปผล
  (เหตุผลไม่ชัด, quote-grounding ล้มเหลว, หรือ confidence ต่ำ)
- `INSUFFICIENT_EVIDENCE` — เอกสารหลักฐานไม่พอ ไม่ใช่ `INAPPROPRIATE`
  โดยตั้งใจ

แถวที่ตัวจำแนก deterministic ส่งต่อให้ LLM จะต้องผ่าน quote-grounding
แบบ fail-closed 6 ชั้น หาก grounding ล้มเหลว ระบบจะเก็บผลเป็น
`NEEDS_REVIEW` ไม่ใช่เผยแพร่ผลตัดสินสุดท้าย

## สถานะ Phase 1

ตอนนี้ `bba audit` ทำเฉพาะ **ส่วน ingest** ของ pipeline คือแปลงชุด CSV
จาก HOSxP เป็น DuckDB + Parquet ภายใต้
`$BBA_DATA_DIR/audit/<run_id>/` พร้อมกลไก idempotency ระดับ run
ส่วนการวิเคราะห์ด้วย LLM (deterministic classifier → evidence bundle →
de-id → prompt → batch → quote grounding → audit-store write)
มีโมดูลรองรับแล้ว แต่จะ **เชื่อมเข้ากับ CLI ใน Phase 1.5** ดูรายละเอียดที่
[คำสั่ง audit](/blood_audit/th/operators/audit-command/)

## 12 ขั้นตอนของ pipeline

<PipelineDiagram />

## ขั้นถัดไป

<AudienceTiles
  tiles={[
    {
      label: "ผู้ดูแลระบบ",
      audience: "KCMH IT",
      description:
        "ติดตั้งแพ็กเกจ ตั้งค่า environment variables รัน bba ingest และ bba audit แล้วส่งชุดข้อมูลออดิทให้คณะกรรมการ QI",
      primaryHref: "/blood_audit/th/operators/install/",
      primaryText: "ติดตั้ง",
      secondaryHref: "/blood_audit/th/operators/first-audit-run/",
      secondaryText: "รันออดิทครั้งแรก",
    },
    {
      label: "นักพัฒนา",
      audience: "ผู้ดูแล src/bba/",
      description:
        "สถาปัตยกรรม 12 ขั้นตอน, ตัวจำแนก 3-tier Hb แบบ deterministic และ quote-grounding 6 ชั้นแบบ fail-closed",
      primaryHref: "/blood_audit/th/developers/architecture/",
      primaryText: "สถาปัตยกรรม",
      secondaryHref: "/blood_audit/th/reference/module-glossary/",
      secondaryText: "Module glossary",
    },
    {
      label: "ทางคลินิก",
      audience: "คณะกรรมการ QI",
      description:
        "ขอบเขตของการออดิทย้อนหลัง ค่า classification ทั้ง 5 แบบ และรายงานสำหรับ QI โดยย้ำว่าไม่ใช่ Software as a Medical Device",
      primaryHref: "/blood_audit/th/clinical/what-this-is/",
      primaryText: "นี่คืออะไร",
    },
  ]}
/>
