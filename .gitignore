# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python

# Virtual environment
.venv/
venv/
env/

# Type-checker / test caches
.mypy_cache/
.pytest_cache/
.ruff_cache/
.hypothesis/
.coverage
.coverage.*
htmlcov/
coverage.xml

# Build artefacts
build/
dist/
*.egg-info/

# Editor / OS
.DS_Store
.vscode/
.idea/
.claude/

# Project — encrypted clinical data MUST NEVER be committed
data/encrypted/
data/raw/
*.csv
!tests/fixtures/**/*.csv
# Vendored public MSBOS reference schedule (ICD-9 -> blood-order recommendation,
# NOT patient data) — required in-repo by the pre-op reservation arm (#163).
!src/bba/preop_reservation/data/OPBloodLimit_by_icd9.csv
# Vendored OPRTACT->ICD9 bridge (billing-code -> procedure-code mapping,
# NOT patient data) — required in-repo by the planned-op picker v2 (#197).
!src/bba/preop_reservation/data/oprtact_icd9_bridge.csv

# Secrets — never commit
.env
.env.*
!.env.example

# Claude Code — per-machine overrides (project-tracked settings.json IS committed)
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/.ralph-loop.local.md
.claude.local.md
